<% flash.each do |key, message| %>

    <div class="container" id="fade-out-container">
        <div class="row">
            <div class="col s12">
                <div class="card-panel red lighten-2 white-text">
                    <%= message %>
                </div>
            </div>
        </div>
    </div>

<% end %>